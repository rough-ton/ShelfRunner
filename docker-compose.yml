services:
  shelfrunner:
    build: .
    container_name: shelfrunner
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ${HOST_DOWNLOAD_DIR:-/mnt/downloads}:/downloads:ro
    network_mode: bridge